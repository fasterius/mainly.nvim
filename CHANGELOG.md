# Changelog

## [v1.0.0]

- Disambiguation of `main.nf` and `main.nf.test` files, including validation of
  component/pipeline directory structure.
- Configuration through `allowed_sources` and `include_component`, with
  defaults.
- Requires Neovim >= `0.11.0`.
